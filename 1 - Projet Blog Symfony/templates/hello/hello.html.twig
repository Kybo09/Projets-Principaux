{% extends "base.html.twig" %}
    {% block body %}
        {% if name %}
            <div class="container">
                <div class="row">
                    <div class="col-sm">
                        <h1>Hello {{ name }}</h1>
                    </div>
                    <div class="col-sm">
                        One of three columns
                    </div>
                    <div class="col-sm">
                        One of three columns
                    </div>
                </div>
            </div>

        {% endif %}
    {% endblock %}
